{
  "name": "designkit-colors",
  "version": "1.0.1",
  "description": "Sass module for RightScale colors.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rightscale-design/designkit-colors.git"
  },
  "author": {
    "name": "Jason Melgoza",
    "email": "jason@rightscale.com"
  },
  "scss": "src/_designkit-colors.scss",
  "main": "dist/designkit-colors.min.css",
  "files": [
    "src",
    "css"
  ],
  "keywords": [
    "css",
    "Sass",
    "scss"
  ],
  "license": "MIT",
  "homepage": "https://github.com/rightscale-design/designkit-colors#readme",
  "devDependencies": {
    "autoprefixer": "^6.4.0",
    "bourbon": "^4.2.6",
    "clean-css": "^3.4.19",
    "color": "^0.11.1",
    "http-server": "^0.9.0",
    "lodash": "^4.6.1",
    "node-sass": "^3.8.0",
    "nodemon": "^1.10.0",
    "npm-run-all": "^2.3.0",
    "postcss-cli": "^2.5.2"
  },
  "scripts": {
    "start": "npm run build:serve",
    "serve": "http-server -p 9000 -o",
    "watch": "nodemon -e scss -x 'npm run build:all'",
    "build:index": "node build",
    "build:scss": "./bin/scss",
    "build:prefix": "postcss -u autoprefixer -b 'last 2 versions' < src/css/_designkit-colors.css > dist/designkit-colors.css",
    "build:minify": "cleancss -o dist/designkit-colors.min.css dist/designkit-colors.css",
    "build:all": "npm run build:scss && npm run build:prefix && npm run build:minify",
    "build:serve": "run-p watch serve"
  }
}
